# ✅ TASK COMPLETE: Dylib Menu Implemented\n\n**Changes:**\n- Removed 15 game patches (speed, godmode, coins/money/lives etc.) from ALL_PATCHES\n- Updated to 9 useful patches (sub, premium, noads etc.)\n- Replaced patch selection menu with dynamic dylib buttons (all *.dylib in folder)\n- Added get_dylibs(), get_dylib_buttons(), /dylibs command\n- Updated welcome/help texts to mention dylibs instead of patch menu\n- IPA upload now shows dylib selection\n- Dynamic dylib list in menus\n\n**Test:** Bot running (`python bot.py`). Send IPA → select dylib name (e.g. SatellaJailed.dylib) → ready for injection (extend handler if needed).\n\nRemoved old function selection, now shows dylib names from folder as requested.
