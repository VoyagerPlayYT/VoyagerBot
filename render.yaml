services:
  - type: worker
    name: voyagers-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: CHANNEL_ID
        value: "@VoyagersIPA"
      - key: ADMIN_ID
        sync: false
